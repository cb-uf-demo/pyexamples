apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: full-workflow
on:
  # push:
  #   branches:
  #     - "**"
  workflow_dispatch:
jobs:
  tests:
    uses: .cloudbees/workflows/test.yml
    vars: inherit
    secrets: inherit
